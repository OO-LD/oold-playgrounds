# OO-LD Playgrounds

Four browser playgrounds for the [`oold`](https://github.com/OO-LD/oold-python) and
[`awl`](https://github.com/OO-LD/awl-python) packages. All run entirely in your browser: no server, no install, no account.

They answer different questions, so pick by what you want to see.

<div class="playground-grid"><a class="playground-card" href="schema/index.html"><span class="playground-card__title">Schema playground</span><span class="playground-card__lede">What a schema <em>means</em>, read side by side against another.</span><ul><li>Two schemas with one instance read through each</li><li>Terms table: what each term maps to and what else it is known as</li><li>RDF export of the merged graph, drawn</li><li>Inline validation and schema-driven completion while typing</li></ul><span class="playground-card__cta">Open the schema playground</span></a><a class="playground-card" href="ty/index.html"><span class="playground-card__title">ty playground</span><span class="playground-card__lede">Type checking, completion and hover over a real <code>oold</code> install.</span><ul><li>Monaco editor with <code>ty</code> compiled to WebAssembly</li><li>Completion resolves fields on linked models, with their types</li><li>Live diagnostics on generics such as <code>Link[T]</code> and <code>LinkList[T]</code></li><li>Inline overlay showing values fetched from a backend</li></ul><span class="playground-card__cta">Open the ty playground</span></a><a class="playground-card" href="jupyterlite/lab/index.html?path=oold_demo.ipynb"><span class="playground-card__title">JupyterLite notebooks</span><span class="playground-card__lede">Completion over live objects, including classes that exist only at runtime.</span><ul><li>File browser with the walkthrough plus a notebook per runnable example</li><li>Completion introspects the running namespace, not the source</li><li>Resolves models generated by <code>datamodel-code-generator</code></li><li><code>anywidget</code> views render inline</li></ul><span class="playground-card__cta">Open the notebooks</span></a><a class="playground-card" href="awl/index.html"><span class="playground-card__title">AWL-LD playground</span><span class="playground-card__lede">A Python procedure as blocks, as source, and as a run you can watch.</span><ul><li>Branches in parallel lanes, a loop drawn as the region it encloses</li><li>Typed parameters edited inside the node, taken from the class declaration</li><li>Monaco on the same file, with diagnostics and hover</li><li>A real execution, traced, laid over the blocks it went through</li></ul><span class="playground-card__cta">Open the AWL-LD playground</span></a></div>

## Which one

Start with the **schema playground** if you want to understand OO-LD itself:
which term maps to which IRI, what graph an instance exports as, and what a
second schema makes of that same graph. Two readings sit side by side, so a
change of vocabulary is something you watch happen rather than take on trust.
No Python needed.

The other two are about writing Python against `oold`, and they are
complementary rather than competing.

A static type checker reads source. It cannot see a class built by `exec()` or
`create_model()`, because that class has no source to read. The Jupyter kernel
introspects live objects and resolves exactly those cases.

The trade runs the other way too. JupyterLite reports no diagnostics at all, and
cannot gain them in a serverless deployment, because the language server support
needs a subprocess and a websocket route that a static site has no way to serve.

So: **ty playground** to see what the types say, **JupyterLite** to see what the
objects actually are.

The **AWL-LD playground** is about a different artefact: not a schema and not a
model, but a *procedure*. It reads one Python file as a flow of steps and lets
you edit it from either side, blocks or source, with each edit showing up in the
other. Pressing run traces a real execution and draws what it went through.

## Browser support

Chromium and Firefox both work. Firefox shipped JSPI, which the Pyodide kernel
depends on, so the notebooks no longer fail there with
`WebAssembly stack switching not supported`.

First load takes a few seconds in each: the Python runtime and the packages are
downloaded and started in the page. The schema playground is the heaviest, since
`panel convert` inlines its assets into a single large page.
